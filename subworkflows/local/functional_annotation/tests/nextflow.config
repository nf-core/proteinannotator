process {
    withName: INTERPROSCAN {
        ext.args = '--applications Hamap,TIGRFAM,sfld --disable-precalc'
        memory   = 12.GB
        cpus     = 1
    }

    withName: KOFAMSCAN {
        ext.args = '--format detail-tsv'
        memory   = 2.GB
    }
}
